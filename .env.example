# Copy to .env in the project root and fill in the key locally.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_TIMEOUT_SECONDS=45
# Optional public HTTPS origin for a custom domain (no path).
# Render's default onrender.com URL is detected automatically.
PUBLIC_ORIGIN=
